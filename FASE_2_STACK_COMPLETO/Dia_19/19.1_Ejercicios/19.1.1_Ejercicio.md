
## 🧪 Ejercicio 1 – Componente TarjetaProducto con props


### 🎯 ENUNCIADO GENERAL
```bash
Crea un componente llamado TarjetaProducto que muestre información de un 
producto (nombre, descripción, precio e imagen). Este componente debe recibir 
los datos por props y mostrarlos en una tarjeta con estilos de Tailwind.
```

### 🧭 PISTAS PARA RESOLVERLO
```bash
	•	Crea un archivo TarjetaProducto.jsx dentro de src/components.
	•	El componente debe recibir titulo, descripcion, precio e imagen como props.
	•	Usa Tailwind para darle forma de tarjeta (fondo blanco, bordes redondeados,
        sombra, etc.).
	•	Dentro del return, estructura la tarjeta: 
        imagen → título → descripción → precio → botón.
	•	Importa y usa TarjetaProducto dentro del App.jsx para mostrar un 
        ejemplo con datos simulados.
```
### 🧩 PASOS A SEGUIR SIN EL CODIGO
```jsx
// 📄 1. Creamos el componente reutilizable que muestra un producto
// 2. Contenedor con estilos Tailwind para tarjeta: fondo blanco, esquinas redondeadas, sombra, padding
{/* 3. Imagen del producto con ancho completo, altura fija y recorte para que encaje */}
// 4. Texto alternativo para accesibilidad
{/* 5. Título del producto en grande y negrita */}
{/* 6. Descripción en texto más suave */}
{/* 7. Precio destacado en verde */}
{/* 8. Botón de acción: Comprar, con fondo azul y hover animado */}

```
### 🧩 CÓDIGO COMPLETO
```jsx
// 📄 src/components/TarjetaProducto.jsx
// ✅ Componente reutilizable para mostrar un producto
// 📄 1. Creamos el componente reutilizable que muestra un producto

// 👉 Recibe como props: título, descripción, precio, imagen
export default function TarjetaProducto({ titulo, descripcion, precio, imagen }) {
  return (
    // 2. Contenedor con estilos Tailwind para tarjeta: fondo blanco, esquinas redondeadas, sombra, padding
    <div className="bg-white rounded-xl shadow-md max-w-xs p-4">
      
      {/* 3. Imagen del producto con ancho completo, altura fija y recorte para que encaje */}
      <img
        src={imagen}
        alt={titulo} // 4. Texto alternativo para accesibilidad
        className="w-full h-40 object-cover rounded-md"
      />

      {/* 5. Título del producto en grande y negrita */}
      <h2 className="text-lg font-bold text-gray-800 mt-3">{titulo}</h2>

      {/* 6. Descripción en texto más suave */}
      <p className="text-gray-600 mt-1">{descripcion}</p>

      {/* 7. Precio destacado en verde */}
      <p className="text-green-600 font-semibold mt-2">Precio: {precio} €</p>

      {/* 8. Botón de acción: Comprar, con fondo azul y hover animado */}
      <button className="bg-blue-500 text-white w-full mt-4 py-2 rounded hover:bg-blue-600 transition">
        Comprar
      </button>
    </div>
  );
}
```
```jsx
// 📄 9. Importamos el componente que acabamos de crear
import TarjetaProducto from './components/TarjetaProducto'

export default function App() {
  return (
    // 10. Contenedor general de la página: pantalla completa, fondo gris claro, centrado vertical y horizontal
    <div className="min-h-screen bg-gray-100 flex justify-center items-center p-10">
      
      {/* 11. Usamos TarjetaProducto con datos simulados (props) */}
      <TarjetaProducto
        titulo="Camiseta React"
        descripcion="Una camiseta con diseño geek para desarrolladores."
        precio={19.99}
        imagen="https://source.unsplash.com/400x300/?tshirt"
      />
    </div>
  )
}
```